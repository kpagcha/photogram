$('#likes_<%= @post.id %>').html("<%= pluralize(@post.get_likes.size, 'like')%>");
$('#like_button_<%= @post.id %>').removeClass('glyphicon-heart-empty').addClass('glyphicon-heart');
$('#like_button_<%= @post.id %>').parent().attr('href', "<%= unlike_post_path(@post) %>")